import geopandas as gpd
import pandas as pd

class Proximity:
    """Neighbor finding and spatial proximity operations"""
    
    @staticmethod
    def find_point_neighbors(central_point, target_points_gdf, central_key_column, buffer_radius):
        # find_point_neighbors() function content from geospatial.py
        
    @staticmethod
    def find_points_within_polygon(points_gdf, polygon_geom):
        # find_points_within_polygon() function content from geospatial.py