import numpy as np
import pandas as pd
import scipy.signal

class PeakDetection:
    """Peak and trough detection in time-series"""
    
    @staticmethod
    def find_peaks_troughs(data_series):
        # find_peaks_troughs() function content from analysis.py
        
    @staticmethod
    def find_peak_to_peak(data, peak_idx, trough_idx):
        # find_peak_to_peak() function content from analysis.py
        
    @staticmethod
    def evaluate_model_fit(original_series, modeled_series):
        # evaluate_model_fit() function content from analysis.py